(function () {
	
	scrollConverter.activate();

}());

/*$(window).scroll(function (event) {
    var y = window.pageXOffset;

    if (y >=1950) {
    	 firstAnimation();
    }
    y=window.pageXOffset;
});
var firstAnimation = function () {
     $('#car').find('img').delay(1400).animate({
            opacity: 1, 
            left: -50
        }, 'slow'
    );
};*/